/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['assets.aceternity.com', 'drive.google.com', 'ca.slack-edge.com', 'media.licdn.com', 'upload.wikimedia.org'],
  },
}

export default nextConfig;
